# Private credentials — never commit (use secrets.h.example as template)
secrets.h

# Build artifacts
build/
*.bin
*.elf

# Device backups (/export snapshots — device-specific data, not code)
backups/
